# Individual Scripts

The `scripts/` directory contains the first iteration set of scripts that are specific to each function and release channel.

It's recommended to use `repair.sh` in the root of the project repository.
