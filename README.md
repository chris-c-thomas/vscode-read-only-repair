# vscode-read-only-repair
macOS utility script to diagnose and repair VS Code “read-only mode” update failures. Fixes quarantine flags, immutable file flags, ownership drift, and bundle permission issues for both Stable and Insiders builds.
